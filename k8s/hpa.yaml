apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: weather-health-hpa
  namespace: weather-space
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: weather-health-app
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50